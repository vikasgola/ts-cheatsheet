// Compile the code with command
//----------------- tsc main.ts----------------
// After compiling, a file will be generated as output
// of name main.js that will contain the js conversion of this file
// ts to js conversion process called transpilation
// tsc (ts compiler) only deals with the files .ts and .d.ts
// ts program is composed of
// --- modules
// --- Functions
// --- variables
// --- statements and expressions
// --- comments
// ts-keywords: each word below is a keyword
// --- break as any switch
// --- case if throw else
// --- var number string get
// --- module type instanceof typeof
// --- public private enum export
// --- finally for while void
// --- null super this new
// --- in return true false
// --- any extends static let
// --- package implements interface function
// --- new try yield const
// --- continue do catch finally
// semicolons are optional
// =====Types=====
var allInOne = "hello world!"; // any type can have any type and this type is dynamic
var num = 123; // number type for integers and floats
var num = 123.12;
var trueOrFalse = true; // boolean type
var nothing; // void type for functions which returing nothing
